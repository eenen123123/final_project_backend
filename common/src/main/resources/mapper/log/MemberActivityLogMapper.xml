<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.ddit.finalProject.mapper.log.MemberActivityLogMapper">

    <insert id="insertMemberActivityLog" parameterType="kr.or.ddit.finalProject.dto.log.MemberActivityLogDto">
        INSERT INTO MEMBER_ACTIVITY_LOG (
            TRACE_ID, USER_ID, ACTIVITY_TYPE, TARGET_ID, ACTIVITY_IP, STATUS_CODE
        ) VALUES (
            #{traceId},
            #{userId},
            #{activityType},
            #{targetId},
            #{activityIp},
            #{statusCode}
        )
    </insert>

</mapper>
