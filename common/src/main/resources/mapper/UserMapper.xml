<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.ddit.finalProject.mapper.UserMapper">

    <resultMap id="userResultMap" type="kr.or.ddit.finalProject.dto.user.UserDto">
        <result property="loginId" column="LOGIN_ID"/>
        <result property="password" column="PASSWORD"/>
        <result property="name" column="name"/>
        <result property="nickName" column="NICK_NAME"/>
        <result property="role" column="ROLE" typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result property="status" column="STATUS" typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
    </resultMap>

    <select id="existsByLoginId" parameterType="string" resultType="boolean">
        SELECT CASE WHEN COUNT(*) > 0 THEN 1 ELSE 0 END
        FROM USERS
        WHERE LOGIN_ID = #{loginId}
    </select>

    <select id="existsByNickname" parameterType="string" resultType="boolean">
        SELECT CASE WHEN COUNT(*) > 0 THEN 1 ELSE 0 END
        FROM USERS
        WHERE NICK_NAME = #{nickname}
    </select>

    <select id="findByLoginId" parameterType="string" resultMap="userResultMap">
        SELECT LOGIN_ID, PASSWORD, name, NICK_NAME, ROLE, STATUS
        FROM USERS
        WHERE LOGIN_ID = #{loginId}
    </select>

    <insert id="insertUser" parameterType="kr.or.ddit.finalProject.dto.user.UserDto" useGeneratedKeys="true" keyProperty="loginId" keyColumn="login_id">
        INSERT INTO USERS (LOGIN_ID, PASSWORD, name, NICK_NAME, ROLE, STATUS)
        VALUES (#{loginId}, #{password}, #{name}, #{nickName},
                #{role, typeHandler=org.apache.ibatis.type.EnumTypeHandler, jdbcType=VARCHAR},
                #{status, typeHandler=org.apache.ibatis.type.EnumTypeHandler, jdbcType=VARCHAR})
    </insert>

</mapper>
