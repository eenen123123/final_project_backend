<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.ddit.finalProject.mapper.UserMapper">

    <resultMap id="memberTestResultMap" type="kr.or.ddit.finalProject.dto.user.UserDto">
        <id property="userId" column="USER_ID"/>
        <result property="userEnpswd" column="USER_ENPSWD"/>
        <result property="userName" column="USER_NAME"/>
        <result property="userEnrrno" column="USER_ENRRNO"/>
        <result property="userGndrCd" column="USER_GNDR_CD"/>
        <result property="userBrdt" column="USER_BRDT"/>
        <result property="joinDt" column="JOIN_DT"/>
        <result property="userTelno" column="USER_TELNO"/>
        <result property="userEmailAddr" column="USER_EMAIL_ADDR"/>
        <result property="userZip" column="USER_ZIP"/>
        <result property="userAddr" column="USER_ADDR"/>
        <result property="userDaddr" column="USER_DADDR"/>
        <result property="userProfile" column="USER_PROFILE"/>
        <result property="regDate" column="REG_DATE"/>
        <result property="modDate" column="MOD_DATE"/>
        <result property="enable" column="ENABLE"/>

        <!-- 1:N 매핑 -->
        <collection property="memRoles" ofType="string">
            <result column="USER_ROLE_CD" />
        </collection>
    </resultMap>

    <select id="existsByUserId" parameterType="string" resultType="boolean">
        SELECT CASE WHEN COUNT(*) > 0 THEN 1 ELSE 0 END
        FROM MEMBER
        WHERE USER_ID = #{userId}
    </select>

    <select id="findByUserId" parameterType="string" resultMap="memberTestResultMap">
        SELECT
            m.USER_ID, m.USER_ENPSWD, m.USER_NAME, m.USER_ENRRNO, m.USER_GNDR_CD,
            m.USER_BRDT, m.JOIN_DT, m.USER_TELNO, m.USER_EMAIL_ADDR, m.USER_ZIP,
            m.USER_ADDR, m.USER_DADDR, m.USER_PROFILE, m.REG_DATE, m.MOD_DATE, m.ENABLE,
            r.USER_ROLE_CD
        FROM MEMBER m
        LEFT JOIN MEMBER_ROLE_MAPPING r ON m.USER_ID = r.USER_ID
        WHERE m.USER_ID = #{userId}
    </select>

    <insert id="insertUser" parameterType="kr.or.ddit.finalProject.dto.user.UserDto" useGeneratedKeys="true" keyProperty="userId" keyColumn="USER_ID">
        INSERT INTO MEMBER (USER_ID, USER_ENPSWD, USER_NAME)
        VALUES (#{userId}, #{userEnpswd}, #{userName})
    </insert>

</mapper>
